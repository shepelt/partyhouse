# PartyHouse Docker Environment Configuration
# Copy this file to .env and configure for your deployment

# Public URL for the PartyHouse application
# Used by Meteor for absolute URLs and OAuth callbacks
ROOT_URL=https://your-domain.com

# Host port to expose PartyHouse on
# The container always runs on port 4000 internally
# This configures the host port mapping (e.g., PORT=8080 maps to localhost:8080)
PORT=4000

# Optional: Meteor settings as JSON string
# METEOR_SETTINGS='{"private":{"apiKey":"your-key"}}'

# ============================================
# Tailscale Configuration (Optional)
# ============================================
# Only needed if using Tailscale for secure remote access
# Enable with: docker-compose --profile tailscale up -d

# Tailscale Auth Key (generate at https://login.tailscale.com/admin/settings/keys)
# Use a reusable key with --ephemeral flag for containers
# TS_AUTHKEY=tskey-auth-xxxxx

# Hostname for this device on your Tailscale network
# TS_HOSTNAME=partyhouse

# Tailscale cert domain (leave as-is, will be auto-filled)
# TS_CERT_DOMAIN=${TS_HOSTNAME}
